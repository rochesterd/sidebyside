# PyInstaller spec for settings.py, the technician tool -- see
# PACKAGING.md and app.spec's comment for the full reasoning (identical
# here: same empirically-confirmed dependency set, same upx=False
# rationale). Confirmed by actually running the frozen settings.exe --
# opens cleanly with no import errors.

a = Analysis(
    ['../settings.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='settings',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=False,
    upx_exclude=[],
    name='settings',
)
